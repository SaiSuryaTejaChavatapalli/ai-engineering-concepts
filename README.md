## Basic Setup

Initialize UV project `uv init`

Create virtual environment `uv venv`

Activate virtual environment`.venv\Scripts\activate`

Install dependencies mentioned in requirements.txt `uv add -r requirements.txt`
